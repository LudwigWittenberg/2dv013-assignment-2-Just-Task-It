apiVersion: v1
kind: Service
metadata:
  name: just-task-it-loadbalancer
  labels:
    app: just-task-it
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: just-task-it
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
  - port: 3000
    targetPort: 3000
    protocol: TCP

